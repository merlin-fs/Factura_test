using System;

namespace Game.Core.Units
{
    /// <summary>
    /// Базовий інтерфейс навички юніта. Всі навички реалізують цей інтерфейс.
    /// </summary>
    public interface ISkill : IDisposable
    {
        /// <summary>
        /// Повертає незалежну копію навички для нового екземпляра юніта.
        /// </summary>
        ISkill Clone();

        /// <summary>
        /// Прив'язує навичку до юніта-власника після впровадження залежностей.
        /// Викликається автоматично в <c>BaseUnit</c>. За замовчуванням нічого не робить;
        /// перевизначається навичками, що потребують доступу до власника.
        /// </summary>
        /// <param name="owner">Юніт-власник навички.</param>
        void Bind(Unit owner) { }
    }
}
